# Optional. Leave unset and the site works fine with its built-in content.
# Set these to the SAME Supabase project used by the admin panel to make
# the website pull live content (courses, photos, testimonials, settings)
# instead of the bundled defaults.
#
# Get these from: Supabase Dashboard -> Project Settings -> API
VITE_SUPABASE_URL=xebleaqktakzosahobxv
VITE_SUPABASE_ANON_KEY=sb_publishable_us55WXeA33CpclPhXcKoHA_nefArFai
